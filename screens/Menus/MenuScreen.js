import { View, Text } from 'react-native';
import React from 'react';

function Menu(props) {
  const { navigation } = props;

  return (
    <View>
      <Text>menus
      </Text>
    </View>
  );
}

export default Menu;
